// Validation limits shared by server-side checks and form markup. Dependency-free, so client-safe.
export const MIN_PASSWORD_LENGTH = 12;
export const CONNECTION_NAME_MAX = 80;
/** Longest search term a monitoring list accepts, both in the URL and in the input that writes it. */
export const SEARCH_MAX = 100;
